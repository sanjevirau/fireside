# fireside CLI

This package will contain the single `fireside` emulator-suite binary. The
Phase 0 binary exposes the command shape and reports that service startup is not
implemented yet; it does not claim protocol compatibility.

See the repository `README.md` and `DESIGN.md` for status and contracts.
