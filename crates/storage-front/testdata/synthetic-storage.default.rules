rules_version = '2';
// Synthetic owner-scoped bucket; all unmatched paths are denied.
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{uid}/{path=**} {
      allow read, write: if request.auth != null &&
        (request.auth.uid == uid || request.auth.token.admin == true);
    }
  }
}
