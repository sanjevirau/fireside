rules_version = '2';
// Independent synthetic public-read bucket with administrator-only writes.
service firebase.storage {
  match /b/{bucket}/o {
    match /{path=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}
