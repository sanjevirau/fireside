[package]
name = "fireside-rules-runtime"
description = "Shared Security Rules loading, authentication, and store policy runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish = false

[dependencies]
base64.workspace = true
fireside-core-store = { path = "../core-store" }
fireside-query-engine = { path = "../query-engine" }
fireside-rules-engine = { path = "../rules-engine" }
serde_json.workspace = true

[lints]
workspace = true
