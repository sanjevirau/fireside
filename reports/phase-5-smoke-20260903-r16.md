# Phase 5 final Mac runner — Linux diagnostic r16

**Official browser: 9/9 passed. Complete two-stack smoke: failed at the first
short-soak catalogue read. No full-data gate or tag was started.**

Candidate `20165b0c4191fa982c49b372a815713c2f731abd` used the unchanged Twodart
revision `6bda5bf29b2399017d2a872e8f3fc1a15d073a54`. Before launch,
[CI 33710918981](https://github.com/sanjevirau/fireside/actions/runs/33710918981)
had green Phase 5 harness and Rust quality jobs, satisfying the harness-only
pre-smoke tier. Local typecheck and 64 harness tests passed. This is not a claim
that the entire CI matrix or Phase 5 gate passed: one SDK cell encountered a
ChromeDriver download timeout during dependency installation.

The supplied final browser patch was applied without additional runner edits.
Its resulting SHA-256 is
`ad61e2e6720abe5e53c745ec264c94166ccd3ff9662c84c1655062c9dd0258cc`.
Frozen manifest SHA-256 remains
`27f643a6e6b5b060bb548359584a133e1bec937b06eb3b3f91982910a985faaa`.

Host: AMD Ryzen 5 2600, 12 logical CPUs, 16,146,870,272 bytes RAM, Ubuntu Linux
7.0.0-30-generic; Java 26.0.2.1 and the official Firebase stack pinned by the
manifest. The tiny input remained 5 files / 163,041 bytes. Launch was
2026-09-03 11:24:15 MYT; evidence directory names retain the planned 11:25 label.

## Browser result

All nine journeys passed, including the real .NET download and its non-trivial
PPTX `PK` signature assertion. Page errors: **0**. Gating request failures: **0**.
Required first-party HTTP failures: **0**. Skips: **0**. Admin pages all returned
200. The export status became `completed` at 11:26:41.467 MYT, after creation at
11:26:14.724; the full export journey took 31.366 seconds.

The 11 console-error events remain recorded, not erased; browser assertion
success does not claim a silent console. Exact `net::ERR_ABORTED` navigation and
long-poll cancellations remain in the request classes and verbatim sidecar.

- [Exact browser result](phase-5-metrics/two-tier-smoke-20260903T1125+0800-20165b0-r16/browser-official-initial.json)
- [Verbatim diagnostics and export status bodies](phase-5-metrics/two-tier-smoke-20260903T1125+0800-20165b0-r16/browser-official-initial.json.diagnostics.jsonl)

## Short-soak failure

The soak started at 11:27:09.382 and failed at 11:27:15.133 MYT, before any memory
sample was collected. The tiny browser seed contains no `premade-templates`
document, but the unchanged soak requires a nonempty `premade-templates.limit(1)`
query. Its error is:

```text
Twodart catalog read returned no premade templates
```

The original soak stored hashes rather than readable errors. Reconstructing the
complete stack from the exact source and Node async frames reproduces recorded
SHA-256 `6fa25af5ea7819968f2f38b1bcec599034a5c7a8699c4cc3b7fd16418d6a402d`
exactly. This attribution and its verification test are frozen before the fix in
the [catalogue precondition fixture](../conformance/fixtures/phase5/smoke-soak-catalog-precondition.json).
The other recorded error hash is retained without an unsupported attribution.

[Exact soak evidence](phase-5-metrics/two-tier-smoke-20260903T1125+0800-20165b0-r16/soak-smoke-official.json)
is a failure, not a 60-second pass. Fireside did not start. Automatic export-first
cleanup completed; subsequent checks found owned stack processes 0, gate
listeners 0, no remaining tmux session, failed units 0, and OOM/resource kills 0.
The export is retained on Linux under `exports/official/smoke/` for this attempt.

## Health and prior rejected launch

r15 was rejected before any stack workload because its internal preflight saw
swap-in samples `[0,0,4]`. That rejection is
[preserved separately](phase-5-metrics/two-tier-smoke-20260903T1123+0800-20165b0-r15/two-tier-smoke-20260903T1123+0800-20165b0-r15.log).
r16 had a fresh, successful quiescent preflight. Later startup/browser compilation
did exhibit swap activity; a sampled Next.js process had about 7.78 million KiB
RSS. This is not a zero-swap soak claim: the soak never reached memory sampling.
The zero-swap rule remains unchanged and still must pass in the complete smoke
and immutable gate.

The next harness correction supplies one owned catalogue row only for an empty
diagnostic-smoke catalogue, before measurement, and removes it afterward. It
does not change the browser runner, full-data input, catalogue query, schedule,
or thresholds. Both stacks must complete the entire cheap smoke on the next
exact CI-qualified candidate before any full-data attempt.
