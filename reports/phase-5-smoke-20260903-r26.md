# Phase 5 schema-v3 cheap smoke r26

Status: **FAIL — Fireside dashboard did not render a deck link**. Official
completed all nine journeys and its 60-second soak. Fireside completed login,
then timed out in journey 2. The r25 `field "data" is not available on this value`
error did **not** recur. Root cause of the application-path timeout is not yet
established; this is not evidence that Phase 5 passed or that the full dashboard
issue was repaired.

R26 is **attempt 1 of at most 8** under the authorization issued after r25.
Both stacks completed automatic export-first shutdown. The controller exited 1
and **did not launch the full-data gate**. No active workload was intervened in.

## Exact candidate

- Candidate: `a6c66b493480a6b716c9f32312cd52733a61dad0`, general potential-result-set
  query authorization. Oracle fixture commit `34c8aed` precedes the repair.
- [Candidate CI 33752732445](https://github.com/sanjevirau/fireside/actions/runs/33752732445)
  passed all seven jobs before launch, including native/browser oracle replay.
  [Exact CI record](phase-5-metrics/two-tier-smoke-v3-20260903-a6c66b4-r26/supplemental/ci-a6c66b4-seven-jobs.json).
- Fresh Linux release build exited 0 at `2026-09-03T20:14:03+08:00`, compilation
  1m15s. Binary SHA-256:
  `020e39595ac4dd610367fd3175a7cd2d764cc650db5e53455b112ae638b39c24`.
- Frozen manifest SHA-256:
  `fe9d44c1edb6105d6edc9f0ab3b3251cb34929b7b6113e559ff9a2558ad7b957`.
- Protected browser runner SHA-256 remains
  `ad61e2e6720abe5e53c745ec264c94166ccd3ff9662c84c1655062c9dd0258cc`.
- Twodart remains `6bda5bf29b2399017d2a872e8f3fc1a15d073a54`, unmodified.
- Controller started `2026-09-03T20:14:40+08:00`; failure recorded
  `2026-09-03T12:22:32.607Z`; controller exited `2026-09-03T20:23:24+08:00`.
- AMD Ryzen 5 2600, 12 logical CPUs, 16,146,870,272 bytes RAM,
  Linux 7.0.0-30-generic x64. Node 24.20.0, Java 26.0.2.1, Rust 1.98.0,
  firebase-tools 15.22.0, official Firestore 1.21.0, Chrome 150.0.7871.124.
  The tiny base is five files / 163,041 bytes plus the unchanged synthetic seed.
  Official smoke uses its default heap, not the full-data `-Xmx8g` comparison.

## Side-by-side diagnostic measurements

| Check | Official | Fireside |
| --- | ---: | ---: |
| Last emulator condition's first-ready observation | 14.781 s | 12.176 s |
| Last application condition's first-ready observation | 26.373 s | 30.626 s |
| Raw / aliased cache | 200 valid JSON / 200 valid JSON | 200 valid JSON / 200 valid JSON |
| Storage alias registered for exact port | Pass | Pass |
| Journeys completed / skipped | 9/9 / 0 | 1/9 / 0 |
| Page errors | 0 | 0 |
| Gating request failures / required HTTP failures | 0 / 0 | 0 / 0 |
| Short soak | Pass, 60 s | Not started |
| Swap-in / swap-out pages in soak | 860 / 0 | Not measured |
| Residual swap at window start / end, bytes | 1,187,393,536 / 1,080,500,224 | Not measured |
| Sampled whole-stack peak PSS, bytes | 10,490,150,912 | Not measured |
| Listener delivery p50 / p99 | 33 / 71 ms | Not measured |
| Export-first shutdown exit | 0 | 0 |

Readiness is a ledger observation, not a controlled cold-start benchmark. The
three-sample short soak is diagnostic, not the two-hour gate or a performance
winner. Missing values are not zero. The [PSS table](phase-5-metrics/two-tier-smoke-v3-20260903-a6c66b4-r26/soak-comparison.md)
records Next.js at 7,558,986,752 peak PSS bytes and the main Java process at
599,286,784. Independent per-PID peaks are not simultaneous totals.

The official soak completed 480 token writes / 24 batches, 12 gateway writes,
eight run/case writes, four catalogue reads, two Storage cycles and two safe
function dispatches. All 500 expected listener deliveries arrived. Errors,
stalls, gaps, acknowledged-state mismatches, duplicate observable effects,
cleanup errors, failed units and OOM/resource evidence were zero. Both stack
preflights drained swap and recorded three zero-activity samples; swappiness
stayed 30. Swap during soak remains measurement-only.

## Failure and bounded diagnostic follow-up

The [runner log](phase-5-metrics/two-tier-smoke-v3-20260903-a6c66b4-r26/logs/browser-fireside-initial.log)
records `page.waitForSelector: Timeout 180000ms exceeded`, waiting for
`a[href^="/presentation/"]`. Document and router remained `/home/recent`, startup
spinner count was zero, and the recorded Next.js overlay count was one.
The loader-only DOM record is empty. There is no retained full DOM or Listen
request/response summary from this attempt, so it cannot distinguish an unissued
query, a listener problem, or rendering/state behavior.

The two verbatim console errors are `Provider's accounts list is empty.` and
`[GSI_LOGGER]: FedCM get() rejects with AbortError: signal is aborted without reason`.
They are observations, **not an established explanation** for the missing deck.
No allowlist or verdict was changed. Cache requests succeeded and the browser
logged successful cache fetches and received cache-WebSocket WELCOME/PING frames.
No login-routing change is justified.

After preserving the failed run, an isolated **read-only diagnostic** imported
the exact Fireside export in memory with the exact captured Twodart rules into
Java 1.21.0 and the candidate's local debug build. Both returned the same single
deck through owner/user native queries, a browser owner-query listener, and a
four-target browser session (user, license, user/read/general, owner query).
[Content-free observations](phase-5-metrics/two-tier-smoke-v3-20260903-a6c66b4-r26/supplemental/isolated-query-results.json)
retain hashes, counts and field names only. This confirms that the saved deck is
present and that these specific queries authorize and deliver; it does **not**
replay the failed Twodart application's authentication/timing/session sequence.
It is neither another acceptance smoke nor a new product oracle fixture.

The next permitted correction is the [diagnostic-only amendment](../conformance/fixtures/phase5/r26-observability-gap-contract.json):
record Listen request shapes and completed response summaries, plus final DOM
and overlay text only for the synthetic smoke. Headers, query values, resume
tokens and document contents are excluded; identities/OTPs remain redacted.
It is outside the protected runner, changes no acceptance condition, and must
pass tests/CI before another bounded smoke. The same-root-cause stop condition
remains in force; no new product fix is claimed from r26 alone.

## Preservation and remaining work

All 27 original checksum entries verified on Linux and after pull. Original
harness evidence remains byte-identical; supplemental CI/build/controller and
post-exit observations use a separate checksum manifest. Raw service logs,
runtime regular files and both exports remain in a private local backup and on
the host; credentials and user identities are not published. Non-regular Unix
sockets are not part of that backup.

[Post-exit checks](phase-5-metrics/two-tier-smoke-v3-20260903-a6c66b4-r26/supplemental/post-exit-health.json)
found no owned stack/gate processes or Phase 5 listeners, no failed units, and
no current-boot OOM/resource/hardware matches. Residual swap was 5,392 KiB with
zero activity in the two post-exit steady samples. No full-data directory exists.
The failure prevented final lifecycle/parity/synthetic-artifact acceptance
checks; shutdown success is not a substitute for those unrun checks.

Both complete cheap-smoke stacks, the immutable full-data gate, sequential
two-hour soaks, restart journeys, fresh-colleague/default and official-fallback
acceptance, parity and final regressions remain required. No Phase 5 pass, tag,
or Phase 6 work is authorized by these partial results.
