# Phase 5 cheap smoke r17 — browser pass, soak initialization failure

Candidate `0854237fbfd3364ae778123f07b41328a2f16166` passed typecheck, all 69 local
harness tests, and the required Phase 5 harness / Rust CI jobs in
[33711764590, attempt 2](https://github.com/sanjevirau/fireside/actions/runs/33711764590/attempts/2)
before launch at 2026-09-03 11:38:58 MYT. Twodart, the final supplied browser
runner, and the frozen manifest were unchanged from r16.

The [official browser result](phase-5-metrics/two-tier-smoke-20260903T1138+0800-0854237-r17/browser-official-initial.json)
again passed **9/9**, including a real PPTX download, with page errors 0, gating
request failures 0, required HTTP failures 0, and no skips. The optional image
variant `high.png` returned a 404, retained in the
[verbatim diagnostics](phase-5-metrics/two-tier-smoke-20260903T1138+0800-0854237-r17/browser-official-initial.json.diagnostics.jsonl).
It is not represented as a zero-total-HTTP-error run.

The smoke-only catalogue correction allowed the first catalogue read and 20
token writes to complete. The soak then failed before memory sampling with:

```text
ReferenceError: Cannot access 'frozenDispatchBody' before initialization
```

The [exact soak evidence](phase-5-metrics/two-tier-smoke-20260903T1138+0800-0854237-r17/soak-smoke-official.json)
now contains the full readable stack. The cache variable was declared below the
top-level `await main()`, so the function-dispatch worker accessed it before
initialization. This is a soak harness defect, not a browser or emulator failure.
Cleanup of the owned catalogue seed reported no errors. The gate performed
export-first shutdown; post-run checks found no gate tmux session or listeners,
failed units 0, and current-boot OOM/resource evidence 0.

The fail-fast gate did not launch Fireside, and no full-data attempt followed.
The [runtime-boundary fixture](../conformance/fixtures/phase5/soak-runtime-boundaries.json)
preserves this failure before the fix. A source audit also identified incomplete
end-of-window sampling; that coverage is strengthened without changing duration,
workload, or the zero-swap rule. Both-stack smoke must still pass before the
immutable full-data gate.
