# Phase 5 r34 preflight failure

Status: **INVALID-HARNESS; NO STACK STARTED**

Candidate `74113adf310d9beb6369cf4b1fe7da6471c8dc40` passed all seven
required CI jobs and a fresh Linux release build. The cheap gate then stopped
before its authorized swap drain and before either emulator stack started. A
single `/proc` scan observed one process with its current working directory at
the official Twodart checkout; two later read-only inspections found no such
process and no gate listener.

## Frozen boundary

- Manifest SHA-256: `fe9d44c1edb6105d6edc9f0ab3b3251cb34929b7b6113e559ff9a2558ad7b957`
- Protected browser runner SHA-256: `ad61e2e6720abe5e53c745ec264c94166ccd3ff9662c84c1655062c9dd0258cc`
- Twodart revision: `6bda5bf29b2399017d2a872e8f3fc1a15d073a54`
- Candidate CI: GitHub Actions run `33834124800`, seven jobs green
- Release binary SHA-256: `eb967542b64725d62aaaaaf74d294c150fe7725fbe9bf08b72bf938248956ba8`
- [Exact r34 evidence](phase-5-metrics/two-tier-smoke-v3-20260904-74113ad-r34/)

## Observation

The official preflight ran from `2026-09-04T04:34:33.276Z` through
`04:34:33.327Z`. Its one scan reported PID `565969` at
`stack-official`; r34 did not capture that process's command identity. The
safety check rejected the drain, so `swapoff` and `swapon` were not invoked.
The controller exited 1, no browser journey or soak began, and the conditional
full-data directory was never created.

At `12:43:03+08:00` and `12:56:02+08:00`, read-only inspections found no
matching Phase 5 checkout process and no gate listener. The host remained
running with SSH active, zero failed units, and zero current-boot OOM/resource
evidence. The controlled runtime filesystem passed its 80 GB capacity check.

Two launcher-only failures before the gate invocation are also retained under
`supplemental/launcher-rejections/`: the first lacked permission to create the
new runtime root; the second asserted that its redirected controller log did
not exist. Neither started a workload. The latter was corrected by deleting
only that contradictory assertion.

## Required correction

Quiescence must be established by consecutive empty process samples before any
swap mutation, not inferred from one instantaneous scan. Every non-empty sample
must retain PID, parent PID, elapsed time, command name, command line, and
working directory. A process that persists through the bounded quiescence
window still fails before `swapoff`; a transient observation may be waited out
but is never omitted from the evidence ledger. This changes no manifest,
protected journey, workload, duration, or threshold.
