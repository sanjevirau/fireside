# Phase 5 schema-v3 cheap smoke r28

Status: **FAIL — Fireside journey 7 document comparison**. Official passed all
nine journeys and its 60-second soak. Fireside passed journeys 1–6, then failed
with `.NET export mutated the selected deck`. This is attempt **3/8** after
r25. The full-data gate did not start; this is not a Phase 5 acceptance pass.

Candidate `b1ef52b77846db7c94ab1c37451d977a629d69ac` passed all seven jobs in
[CI 33760383375](https://github.com/sanjevirau/fireside/actions/runs/33760383375).
Fresh Linux release compilation took 1m15s, exiting zero at
`2026-09-03T21:33:41+08:00`, binary SHA-256
`c756e6a8bf04ab4b1efea6605a342d335cab64b4bf3fc248002ada75b117189a`.
Controller launch was `2026-09-03T21:34:03+08:00`; failure was recorded at
`2026-09-03T13:40:43.130Z`; controller exit 1 followed automatic export-first
shutdown at `2026-09-03T21:42:37+08:00`.

Manifest SHA-256 remains
`fe9d44c1edb6105d6edc9f0ab3b3251cb34929b7b6113e559ff9a2558ad7b957`;
protected runner SHA-256 remains
`ad61e2e6720abe5e53c745ec264c94166ccd3ff9662c84c1655062c9dd0258cc`.
Twodart remains `6bda5bf29b2399017d2a872e8f3fc1a15d073a54`.

## Side-by-side diagnostic measurements

Same Linux Ryzen 5 2600 host, 12 logical CPUs, 16,146,870,272 RAM bytes,
Linux 7.0.0-30-generic. Node 24.20.0, Java 26.0.2.1, Rust 1.98.0,
firebase-tools 15.22.0, Firestore JAR 1.21.0, Chrome 150.0.7871.124.
Tiny base: five files / 163,041 bytes plus the unchanged synthetic seed.
The official smoke used its default heap; the full-data `-Xmx8g` retry was not run.

| Measurement | Official | Fireside |
| --- | ---: | ---: |
| All readiness conditions observed ready | 27.480 s | 30.696 s |
| Raw / aliased cache request | 200 valid JSON / 200 valid JSON | 200 valid JSON / 200 valid JSON |
| Exact Storage alias registered | Pass | Pass |
| Completed journeys / skips | 9/9 / 0 | 6/9 / 0 |
| Page errors | 0 | 0 |
| Gating request failures / required HTTP failures | 0 / 0 | 0 / 0 |
| .NET export job | Completed | Completed |
| Journey 7 no-mutation assertion | Pass | Failed |
| Short soak | Pass, 60 s | Not started |
| Swap-in / swap-out pages during soak | 1,355 / 0 | Not measured |
| Residual swap start / end, bytes | 2,175,442,944 / 2,084,483,072 | Not measured |
| Whole-stack sampled peak PSS, bytes | 9,892,724,736 | Not measured |
| Listener delivery p50 / p99 | 31 / 77 ms | Not measured |
| Export-first shutdown exit | 0 | 0 |

These are diagnostic observations, not controlled cold-start benchmarks or a
performance winner. Retained checkout logs can satisfy log-marker conditions;
live ports and probes also had to pass. Three samples over 60 seconds do not
establish an endurance slope. The [complete swap/process table](phase-5-metrics/two-tier-smoke-v3-20260903-b1ef52b-r28/soak-comparison.md)
is preserved; missing measurements are not zero.

Official delivered 500/500 expected listener observations, with errors, stalls,
gaps, state mismatches, duplicate effects, cleanup errors, failed units and
OOM/resource evidence all zero. Work included 480 token writes / 24 batches,
12 gateway writes, eight run/case writes, four catalogue reads, two Storage
cycles and two safe function dispatches. Both preflights drained swap and
observed three steady zero-activity samples; swappiness stayed 30.

## Attribution and limits

The [verbatim failure log](phase-5-metrics/two-tier-smoke-v3-20260903-b1ef52b-r28/logs/browser-fireside-initial.log)
names the assertion at runner line 582. The unchanged runner compares
`JSON.stringify(deckAfter)` with `JSON.stringify(deckBefore)`. It reached that
assertion only after receiving a non-empty download with at least 1,024 bytes
and `PK` ZIP magic. The [recorded .NET responses](phase-5-metrics/two-tier-smoke-v3-20260903-b1ef52b-r28/browser-fireside-initial.json.diagnostics.jsonl)
show HTTP 200 start and completed status, not a failed job hidden in HTTP 200.
The private .NET service log records a 32,971-byte completed/downloaded artifact.
Slides and Storage no-mutation checks after the failed deck assertion were not
reached. Official reported two export steps; Fireside reported one. Final
cross-stack state parity was not reached and is not claimed.

A separate read-only diagnostic imported each saved stack export into Java
1.21.0 and the unfixed Fireside binary, without the app or exporter. Twenty SDK
reads per combination gave:

| Imported export | Read server | Distinct exact JSON strings | Distinct canonical values | Distinct update timestamps |
| --- | --- | ---: | ---: | ---: |
| Official | Java | 1 | 1 | 1 |
| Official | Fireside | 20 | 1 | 1 |
| Fireside | Java | 1 | 1 | 1 |
| Fireside | Fireside | 20 | 1 | 1 |

The [diagnostic observations](phase-5-metrics/two-tier-smoke-v3-20260903-b1ef52b-r28/supplemental/repeated-read-diagnostic.json)
preserve hashes and field-order lists; full values remain private. Fireside's
protobuf field maps are freshly constructed randomized `HashMap`s on response
encoding. This independently reproduces the order-sensitive assertion failure
without any document write: a serialization compatibility gap, not demonstrated
data loss. The original before/after document values were not persisted, so this
diagnostic **does not prove** that no additional app mutation happened during
r28. A corrected full smoke must still pass all unchanged assertions.

The [new permanent synthetic oracle](../conformance/fixtures/document-map-serialization/README.md)
records 224 reads per pinned JAR across Node SDK, native gRPC, REST and both
browser variants. All repeated-read groups are stable; the two JARs' observed
field JSON files are byte-identical. This capture precedes product changes.
The intended repair is generic deterministic map serialization, with semantic
parity and stability tests—not rewriting the protected comparison, imposing
Java's incidental key permutation, or claiming a protobuf map-order guarantee.

R27's invited-users query rejection did not recur: dashboard and four subsequent
journeys passed. There is no justification to revisit login routing, cache,
Twodart rules, application code or the protected runner.

## Preservation and continuation

All 27 original checksum entries verified on the host and after pull; the
original evidence is byte-identical. CI, release build, controller, wrapper,
isolated read diagnostic and post-exit health use supplemental checksums.
Complete exports and service/runtime files are preserved privately in
`/tmp/fireside-phase5-r28-raw.6RFDYH` and on the host. Unix sockets were excluded.
No credentials or raw user identities are published.

The later read-only health check at `2026-09-03T16:41:01.382Z` found no owned
stack processes/listeners, no failed units or current-boot OOM/resource/hardware/
I/O evidence, and 465,326,080 residual swap bytes. No swap setting was changed.
Automatic shutdown success is not full lifecycle/synthetic-artifact acceptance;
those final checks were not reached. No active workload was intervened in.

Continue the authorized product loop: commit the official fixture first,
repair deterministic encoding, add crate/conformance regressions, require seven
green CI jobs on the exact candidate and a fresh Linux release SHA, then r29
unchanged cheap smoke. Full data remains conditional on both complete smoke
stacks passing. Two-hour soaks, restarts, fresh-colleague/fallback acceptance,
parity, regressions and final report/evidence CI remain outstanding. No Phase 5
tag or Phase 6 work.
